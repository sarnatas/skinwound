name: skinwound
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      # --- notebook reattivo ---
      - marimo==0.24.0

      # --- core single-cell / spatial ---
      - scanpy==1.11.5
      - anndata==0.12.19
      - squidpy==1.8.2
      - spatialdata==0.7.3
      - spatialdata-io==0.7.1
      - spatialdata-plot==0.4.0

      # --- clustering (Leiden) ---
      - leidenalg==0.12.0
      - python-igraph==1.0.0

      # --- immagini / utility ---
      - scikit-image
      - openpyxl
